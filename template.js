const firstName = 'Justin';
const lastName = "Timber";
const fullName = firstName +" " + lastName + " is a farmer";
console.log(fullName);

const fullName2 = `${firstName} ${lastName} is a farmer`; //template

const multiLine = `first line
second line
third line`;
console.log(multiLine);

